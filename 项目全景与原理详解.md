# 项目全景与原理详解（零基础版）

> 目标：把"用什么加密、什么算法、训练什么代码"这三件事彻底讲清楚
> 读法：先看第一章（全景），再看你关心的具体部分

---

## 一、一句话总览

> **用「6 轮 SPECK 加密」造出 1000 万道带标准答案的判断题，训练一个「神经网络」学会回答：这两份密文，是来自一对只差一个比特的明文吗？**

| 环节 | 干什么 | 代码文件 |
|---|---|---|
| ① 造密码 | 实现 SPECK32/64 加密算法 | `src/speck/speck.py` |
| ② 出题 | 用加密算法生成 1000 万道"带答案的题目" | `src/data/dataset.py` |
| ③ 造脑子 | 定义一个能处理密文对的神经网络 | `src/model/distinguisher.py` |
| ④ 训练 | 把上面三个串起来，反复做题改错 | `train.py` |

**这四个文件就是你项目的全部核心**，其余（画图、日志、脚本）都是辅助。

---

## 二、回答你的三个问题（先说结论）

| 你的问题 | 答案 |
|---|---|
| **用什么加密？** | **SPECK32/64** —— 美国 NSA 设计的轻量级分组密码，完整版 22 轮，我们只用 **6 轮**（削弱版） |
| **用什么算法？** | 两套：**加密算法** = ARX（模加 + 循环移位 + 异或）；**学习算法** = 深度残差神经网络 + Adam 梯度下降 |
| **训练用什么代码？** | `train.py` 是主入口（训练循环），它调用另外三个文件 |

---

## 三、完整流程图（从明文到训练）

```
【第 ① 步：准备密钥和明文】
        随机生成 64 位密钥 K，随机生成明文 P₀
                    │
    ┌───────────────┴───────────────┐
    │                               │
 标签 = 1                        标签 = 0
（真实对 / right pair）          （随机对 / wrong pair）
    │                               │
 P₁ = P₀ ⊕ 0x0040               P₁ = 完全随机
（只翻转第 6 个比特）            （和 P₀ 毫无关系）
    │                               │
    └───────────────┬───────────────┘
                    ▼
【第 ② 步：用同一个密钥 K，把 P₀ 和 P₁ 各加密 6 轮】
                    │
        C₀ = SPECK6(P₀, K)
        C₁ = SPECK6(P₁, K)
                    ▼
【第 ③ 步：把两份密文变成 64 个比特】
        4 个 16 位字：C₀左、C₀右、C₁左、C₁右
        → 每个字 16 位 → 共 64 个 0/1
                    ▼
【第 ④ 步：喂给神经网络】
        输入 [64 个数]
              ↓
        BitSliced ResNet（10 个残差块）
              ↓
        输出 1 个数（0~1 之间）
              ↓
        越接近 1 = 越确信"这是真实差分对"
                    ▼
【第 ⑤ 步：和标准答案比，算误差】
        真实标签 = 1，网络说 0.8 → 误差 = (1-0.8)² = 0.04
        把所有样本的误差取平均 = loss
                    ▼
【第 ⑥ 步：反向传播，调整网络的 10 万个参数】
        按"学习率"决定每次调多少
                    ▼
【第 ⑦ 步：重复第 ④~⑥ 步】
        每 5000 个样本算一批（1 步）
        2000 步 = 1 个 epoch（看完 1000 万样本一遍）
        200 个 epoch = 完整训练
```

---

## 四、逐个环节详解

### ① 加密：SPECK32/64（`src/speck/speck.py`）

**它是什么**：2013 年 NSA 发布的轻量级分组密码，为 IoT 设备设计。

**名字含义**：`SPECK32/64` = 明文块 **32 位**、密钥 **64 位**。

**结构：ARX**（三种运算的缩写）

| 字母 | 运算 | 代码 |
|---|---|---|
| **A**dd | 模 2¹⁶ 加法 | `c0 = (c0 + c1) & 0xFFFF` |
| **R**otate | 循环移位 | `ror(c0, 7)`、`rol(c1, 2)` |
| **X**or | 异或 | `c0 = c0 ^ k` |

**一轮加密的完整代码**（就 5 行）：
```python
def enc_one_round(c0, c1, k):
    c0 = ror(c0, 7)            # ① c0 右循环移 7 位
    c0 = (c0 + c1) & 0xFFFF    # ② 加上 c1（会溢出回绕）
    c0 = c0 ^ k                # ③ 异或子密钥
    c1 = rol(c1, 2)            # ④ c1 左循环移 2 位
    c1 = c1 ^ c0               # ⑤ c1 异或新的 c0
    return c0, c1
```

**完整版 22 轮**（轮数越多越安全），**我们用 6 轮**（削弱版）。

**为什么用 6 轮？**
- 1 轮：差分痕迹太明显，网络闭着眼都能区分（准确率 1.0）
- 12 轮以上：痕迹被搅匀，网络完全学不到（准确率 0.5）
- **6 轮正好**：痕迹很弱但存在（准确率约 0.79）——这才有研究价值

**这就是"降轮攻击"的含义**：完整版打不动，但研究降轮版能测量密码的**安全裕度**。

---

### ② 出题：训练数据生成（`src/data/dataset.py`）

**每一道"题"的构成**：

```
输入：一对密文（64 个比特）
答案：1（真实对，两个明文只差一个比特）
      0（随机对，两个明文毫无关系）
```

**关键设计：每个样本用不同的随机密钥。**

为什么？防止网络"背密钥"作弊。如果所有样本共用一个密钥，网络可能学会"这个密钥的特征"而不是"差分的规律"。用不同密钥，网络只能学**通用规律**。

**为什么造 1000 万个样本？** 6 轮的信号极弱，样本少了根本学不出来。论文用的就是 10⁷。

**性能关键：GPU 向量化**

官方 numpy 版是逐个样本循环（很慢），我们用张量运算一次处理整个 batch：
```python
subkeys = batch_expand_key(keys, nr)        # 一次算完所有样本的密钥编排
cl0, cr0 = batch_encrypt(plain0l, plain0r, subkeys)
```
**效果：1000 万样本只要 0.7 秒**（论文描述"几秒钟"）。

---

### ③ 造脑子：神经网络（`src/model/distinguisher.py`）

**叫什么**：BitSliced ResNet（位切片残差网络）

**结构**（从输入到输出）：
```
输入 [N, 64]（N 个样本，每个 64 比特）
  ↓ view 成 [N, 4, 16]     ← 4 个 16 位字，让网络看到密码的"字结构"
  ↓ 1×1 卷积扩到 32 通道
  ↓ 10 个残差块（每个 = 卷积+归一化+激活 ×2 + 跳跃连接）
  ↓ 拉平 → 全连接(64) → 全连接(64) → 全连接(1)
  ↓ Sigmoid（压到 0~1 之间）
输出 [N, 1]（每个样本一个分数）
```

**参数量：10 万个**（`depth=10` 时）。

**三个设计理由**（论文 4.2 节）：
1. **输入按"4 个 16 位字"组织** —— 让网络知道密码的字结构
2. **卷积宽度 3** —— 因为模加法的进位只影响相邻比特，差分痕迹是"局部"的
3. **预测头用全连接而非池化** —— 加密数据没有空间对称性，池化提取局部特征是徒劳的

**这是个典型的小型 CNN**，不是大模型（不需要 LLM）。整个网络只有 10 万参数——比一张手机照片还小。

---

### ④ 训练：主循环（`train.py`）

**它做的 6 件事**：
```
1. 解析参数    --rounds 6 --train-size 10000000 --epochs 200 --depth 10
2. 安全检查    check_testvector()  ← 先用官方测试向量验证加密正确
3. 造数据      make_train_data(...)  ← 调用 ②，生成 1000 万样本
4. 建网络      make_distinguisher(depth=10)  ← 调用 ③
5. 训练循环    前向 → 算 loss → 反向 → 更新参数（重复 40 万次）
6. 记录        每个 epoch 存 CSV、TensorBoard、最佳权重
```

**训练循环的核心代码**（就这几行）：
```python
for epoch in range(1, 201):                    # 200 个 epoch
    perm = torch.randperm(n)                   # 打乱样本顺序
    for i in range(0, n, 5000):                # 每次取 5000 个（一个 batch）
        xb, yb = X_tr[idx], Y_tr[idx]
        optimizer.zero_grad()                  # 清空上一批的梯度
        loss = criterion(net(xb), yb)          # 前向：网络猜 + 算误差
        loss.backward()                        # 反向：误差怎么分配给每个参数
        optimizer.step()                       # 按梯度方向微调参数
```

**训练配置**（对照论文）：
| 项目 | 值 | 说明 |
|---|---|---|
| loss 函数 | **MSE**（均方误差） | 把 0/1 分类当回归做，论文原版如此 |
| 优化器 | **Adam** | 自适应学习率的一阶优化算法 |
| 学习率 | **循环**：0.002 ↔ 0.0001，每 10 epoch 一个周期 | 大步探索 + 小步精修交替 |
| batch size | 5000 | 显存限制 |
| epoch | 200 | 把数据集反复看 200 遍 |

---

## 五、"算法"这个词的澄清

你问"用什么算法"，这里其实有**两套完全不同的算法**，容易混淆：

| | 加密算法 | 学习算法 |
|---|---|---|
| 是什么 | **SPECK32/64** | **深度残差网络 + Adam 梯度下降** |
| 干什么 | 把明文搅成密文 | 从数据里学规律 |
| 谁设计的 | NSA（2013） | 神经网络是 1940s 起源，ResNet 是 2015 年（何恺明） |
| 在哪 | `src/speck/speck.py` | `src/model/distinguisher.py` + `train.py` |
| 类别 | 密码学 | 机器学习 / 深度学习 |

**这两者的关系是本项目的灵魂**：用机器学习的工具去分析密码学的问题。这就是"神经密码分析"这个交叉领域的定义。

---

## 六、整个项目的本质（一句话）

> **传统上，密码学家靠手工数学推导来找密码的弱点（比如算差分分布表，8 轮 SPECK 要 300 核心·天、34 GB 数据）。Gohr 的贡献是：让神经网络从数据里自动学出这些弱点，而且效果更好（9 轮攻击的密钥排名低 5 倍）。**

你复现的就是这件事。你的成果：
- 6 轮区分器达到 **0.7873**（论文 0.788）
- 说明网络确实学到了 6 轮 SPECK 的差分规律

---

## 七、下一步该补的基础（按优先级）

| # | 补什么 | 为什么 | 材料 |
|---|---|---|---|
| 1 | **差分分析基础** | 理解"为什么能区分" | `papers/Heys教程中文_2_差分分析.md` |
| 2 | **神经网络反向传播** | 理解 `loss.backward()` | micrograd（154 行，见 `深度学习项目拆解计划.md`） |
| 3 | **SPECK 论文式 (2)** | 对照你的代码 | `papers/Gohr2019中文对照_2_*.md` |
| 4 | **密钥恢复攻击** | 论文最精彩的部分 | `papers/Gohr2019中文对照_4_*.md` |

**最有效的学习方法**：打开 `train.py`，逐行问自己"这行在干什么"，遇到不懂的回来查本文档或问我。
