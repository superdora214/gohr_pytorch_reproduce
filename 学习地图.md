# 项目学习地图（完整梳理）

> 用途：把所有零散知识连成一张网。**看不懂某处就跳到你卡住的那一层**。
> 分层设计：全局 → 组件 → 数据流 → 概念索引 → 核心问题 → 自检清单

---

# 第 0 层：三句话看懂这个项目

1. **密码学侧**：SPECK32/64 是一个完整 22 轮才安全的分组密码。我们把它"削弱"到 6 轮，削弱版会留下数学上的可检测痕迹。
2. **深度学习侧**：训练一个神经网络去看"一对密文"，判断它来自真实差分对还是随机对。6 轮能达到 78.8% 准确率（论文 78.8%）。
3. **攻击侧**：用这个网络做"探测器"，逐个尝试密钥，剥掉最后一轮看网络反应——猜对密钥时网络给高分，于是密钥被找到。

---

# 第 1 层：五个组件（各自是什么、在哪、为什么需要）

```
                    gohr_pytorch_reproduce/
                    │
   ① 加密算法  ────→ src/speck/speck.py
   （密码学基础）       SPECK32/64 的一轮加密、密钥编排、逆轮
                    │
   ② 数据生成  ────→ src/data/dataset.py
   （造训练样本）       用 ① 生成"带答案的密文对"
                    │
   ③ 网络定义  ────→ src/model/distinguisher.py
   （深度学习模型）     BitSliced ResNet：64 比特输入 → 0~1 分数
                    │
   ④ 训练流程  ────→ train.py
   （把 ①②③ 串起来）  200 epoch 循环、算 loss、反向传播、存权重
                    │
   ⑤ 密钥恢复  ────→ key_recovery.py
   （用 ③ 攻击 ①）     试钥剥离 + 打分排序，从 65536 个候选里找真密钥
```

## ① `src/speck/speck.py` —— 加密算法（密码学）

**它是什么**：SPECK32/64 的完整实现，包括三个部分：

| 函数 | 作用 | 关键代码 |
|---|---|---|
| `enc_one_round()` | **一轮加密**（5 行） | `ror → 加 → 异或 → rol → 异或` |
| `expand_key()` | **密钥编排**（从 64 位主密钥生成 22 个子密钥） | 用轮数 i 当子密钥滚动生成 |
| `dec_one_round()` | **一轮解密**（逆运算，攻击时用） | 5 步逆序执行 |

**关键概念**：
- **ARX 结构**：Add（模加）+ Rotate（循环移位）+ Xor（异或）——现代轻量密码的主流设计
- **MASK = 0xFFFF**：16 位掩码。所有运算结果都要 `& MASK` 截断到 16 位（因为"字"只有 16 位）
- **α=7, β=2**：SPECK32 的移位常数
- **轮数（rounds）**：加密内部迭代次数。22 轮 = 完整版；我们研究 5/6/7/8 轮 = 削弱版

**为什么这个文件最基础**：后面所有组件都建立在"加密是对的"这个前提上。所以有**测试向量**验证（`check_testvector()`）——用官方数据对拍，确保一个比特都没错。

## ② `src/data/dataset.py` —— 数据生成（工程 + 密码学）

**它做什么**：造出 1000 万道"带标准答案的判断题"。每道题 = 一对密文 + 标签（0 或 1）。

**核心函数**：`make_train_data(n, rounds)`

```
随机密钥 K + 随机明文 P₀
    │
  标签=1？ → P₁ = P₀ ⊕ 0x0040（真实对：只差一个比特）
  标签=0？ → P₁ = 完全随机（随机对）
    │
用同一个 K 加密 r 轮 → 得到两对密文
    │
转成 64 个比特 → 训练数据
```

**关键设计**：
- **每个样本用不同的随机密钥** —— 防止网络"背密钥"，逼它学通用规律
- **差分 Δ = 0x0040** —— 这个值来自文献（Abed 等人 9 轮特征的第 3 轮差分）
- **GPU 向量化** —— 把密钥编排和加密写成张量运算，10⁷ 样本只要 0.7 秒（官方 numpy 版要慢得多）

**为什么需要这么多数据**：6 轮的差分信号极弱，样本少了根本学不出来（论文用 10⁷，我们实测 20 万样本在 7 轮上完全学不动）。

## ③ `src/model/distinguisher.py` —— 网络定义（深度学习）

**它是什么**：BitSliced ResNet，一个**只有 10 万参数**的小型卷积网络。

**数据流**：
```
输入 [N, 64]                ← N 个样本，每个 64 比特
  ↓ view [N, 4, 16]         ← 4 个 16 位字（面向字表示）★关键
  ↓ 1×1 卷积 → 32 通道
  ↓ 10 个残差块              ← 卷积+BN+ReLU ×2 + 跳跃连接
  ↓ Flatten → 全连接 64 → 64 → 1
  ↓ Sigmoid
输出 [N, 1]                 ← 每样本一个 0~1 的分数（越接近 1 越像真实对）
```

**三个设计决策及其理由**（论文 4.2 节）：
1. **输入按"4 个 16 位字"组织** —— 让网络看到密码的字结构（这里曾经有个 bug，见 `docs/experiments/`）
2. **卷积核宽度 3** —— 因为模加法的进位只影响相邻比特，差分痕迹是"局部"的
3. **用全连接而非池化** —— 加密数据没有空间对称性，池化会破坏位置信息

## ④ `train.py` —— 训练流程（工程）

**它做什么**：把 ①②③ 串起来，跑训练循环。

**六件事**：解析参数 → 验证加密 → 造数据 → 建网络 → 训练循环 → 记录

**核心训练循环**（4 行）：
```python
optimizer.zero_grad()        # ① 清空上一批的梯度
loss = criterion(net(xb), yb) # ② 前向：网络猜 + 算误差
loss.backward()              # ③ 反向：误差怎么分配给每个参数
optimizer.step()             # ④ 按梯度方向微调参数
```

**关键概念**：
- **epoch**：把全部数据过一遍。200 epoch = 反复看 200 遍
- **batch**：一次喂 5000 个样本（显存限制）
- **loss = MSE**：均方误差，衡量"猜得多错"
- **学习率**：每次调参数调多少。循环变化（0.002 ↔ 0.0001，每 10 epoch 一轮）
- **优化器 = Adam**：自适应调整每个参数的步长

## ⑤ `key_recovery.py` —— 密钥恢复攻击（密码分析）

**它做什么**：用训练好的网络当"探测器"，从 65536 个候选密钥里找出真的那个。

**核心机制**：
```
① 生成 64 对明文（攻击者可控制第一轮输出差分）
② 用真密钥加密 8 轮
③ 枚举 2^16 个候选密钥，各剥掉最后一轮
④ 把剥完的结果喂给网络打分
⑤ 得分最高者 = 密钥
```

**打分公式**（论文式 3）：`score(k) = Σ log₂(Z/(1−Z))`
- 把网络的"信心"转成"证据量"并累加
- 真密钥的每对密文都贡献正分 → 总分最高

---

# 第 2 层：数据的完整旅程

## 旅程 A：一次训练（在 `train.py` 里）

```
SPECK 加密算法（①）
    ↓ 被调用
生成 1000 万对密文 + 标签（②）
    ↓ 形状 [10000000, 64]
喂给网络（③）
    ↓ 每批 5000 个
网络输出 [5000, 1] 的分数
    ↓
和标签比 → loss（一个数）
    ↓
反向传播 → 算出 10 万个参数的调整方向
    ↓
按学习率调整参数（④）
    ↓
重复 2000 批 = 1 epoch，重复 200 遍
    ↓
得到权重文件 net6r.pt
```

## 旅程 B：一次攻击（在 `key_recovery.py` 里）

```
训练好的权重 net6r.pt（来自旅程 A）
    ↓
生成 64 对攻击用密文（真的加密 8 轮）
    ↓
枚举 65536 个候选密钥
    ↓ 每个候选：用逆轮函数（①）剥掉最后一轮
得到 65536 × 64 = 419 万个"剥轮后状态"
    ↓ 转成 64 位向量
喂给网络（③）推理
    ↓
得到 419 万个分数
    ↓ 按候选分组、累加（似然比）
65536 个候选得分
    ↓ 排序
真密钥排第几？ → 密钥排名
```

---

# 第 3 层：概念索引（按领域分类）

## A. 密码学概念

| 概念 | 一句话 | 在哪用到 |
|---|---|---|
| **分组密码** | 把固定长度的明文块加密成密文块 | SPECK32/64 本身 |
| **ARX** | Add+Rotate+Xor 三种运算构成的密码结构 | `enc_one_round()` |
| **轮（round）** | 密码内部迭代的次数 | `--rounds 6` |
| **子密钥** | 每一轮用的小密钥 | `expand_key()` |
| **密钥编排** | 从主密钥生成所有子密钥的过程 | `expand_key()` |
| **差分（differential）** | 两份数据的异或差：ΔX = X' ⊕ X'' | Δ = 0x0040 |
| **差分对** | 一对有固定异或差的数据 | 训练数据的"真实对" |
| **MASK** | 16 位掩码，把结果截断到 16 位 | 所有运算后 `& MASK` |
| **模 2¹⁶ 加法** | 会回绕的加法（65535+1 → 0） | `(c0+c1) & MASK` |
| **循环移位** | 首尾相接的移位，不丢比特 | `ror` / `rol` |
| **降轮攻击** | 攻击被削弱的版本以测量安全裕度 | 整个项目 |
| **区分器** | 能区分"真密码输出"和"随机"的装置 | 网络本身 |
| **密钥恢复** | 从密文反推出密钥 | `key_recovery.py` |
| **密钥排名** | 真密钥在候选列表中的排位（0 = 第一） | 攻击的评价指标 |
| **中性比特** | 翻转后不影响差分传播的比特位 | 11 轮攻击（未实现） |

## B. 深度学习概念

| 概念 | 一句话 | 在哪用到 |
|---|---|---|
| **张量（tensor）** | 多维数组，GPU 上并行计算的基本单位 | 所有数据 |
| **卷积（Conv）** | 滑动小窗口，每次只看一小块 | `nn.Conv1d` |
| **卷积核宽度** | 每次看几个相邻位置 | `kernel_size=3` |
| **通道（channel）** | 同一位置上并行的多组特征 | 32 个通道 |
| **残差（Residual）** | 把输出和输入相加，让梯度能流回浅层 | 10 个残差块 |
| **跳跃连接** | 残差的具体实现（`x = out + x`） | `forward()` |
| **池化（Pooling）** | 压缩数据的操作 | ⚠️ **本项目不用**（会破坏比特位置） |
| **BatchNorm** | 把一批数据标准化到统一范围 | 每个卷积后 |
| **ReLU** | 负数变 0，正数不变（引入非线性） | 每个卷积后 |
| **Sigmoid** | 把任意数压到 0~1 | 最后一层 |
| **Flatten** | 把多维数据拉平成一维 | 进入全连接前 |
| **全连接（Dense/Linear）** | 每个输入都连到每个输出 | 预测头 |
| **损失（loss）** | 衡量预测和真值的差距 | MSE |
| **MSE** | 均方误差（差值的平方取平均） | `criterion` |
| **梯度** | loss 对每个参数的偏导数（该往哪调） | `loss.backward()` |
| **反向传播** | 用链式法则从后往前算梯度 | `loss.backward()` |
| **优化器（Adam）** | 根据梯度更新参数的算法 | `optimizer.step()` |
| **学习率** | 每次更新参数调多少 | 循环 0.002 ↔ 0.0001 |
| **循环学习率** | 学习率周期升降（大步探索 + 小步精修） | `cyclic_lr(10, ...)` |
| **epoch** | 把全部训练数据过一遍 | 200 epoch |
| **batch** | 一次喂给网络的样本数 | 5000 |
| **过拟合** | 训练集好但验证集差（背答案了） | 我们验证损失略低于训练损失 |
| **验证集** | 训练时不参与、只用来考试的数据 | 10⁶ 样本 |
| **准确率（accuracy）** | 答对的百分比 | 0.7882 |

## C. 工程/实验概念

| 概念 | 一句话 | 在哪用到 |
|---|---|---|
| **CLI 参数化** | 用命令行参数控制程序行为 | `--rounds 6` |
| **向量化** | 把循环改写成张量运算，一次处理一批 | `batch_encrypt` |
| **种子（seed）** | 固定随机数，让结果可复现 | 未使用（所以每次数据不同） |
| **检查点（checkpoint）** | 训练中间保存的权重 | `net6r.pt` / `last_6r.pt` |
| **断点续训** | 从保存的状态继续训练 | `--resume` |
| **TensorBoard** | 网页版训练监控工具 | `checkpoints/tb_6r/` |
| **消融实验** | 去掉某个组件看效果变化 | 未做（论文里有） |

---

# 第 4 层：三个最容易被忽略的"为什么"

## ① 为什么研究 6 轮而不是 22 轮？

因为**完整版攻不动**（那需要指数级的算力和数据）。研究降轮版本的意义是**测量安全裕度**：

```
1 轮 → 准确率 1.0     （痕迹太明显，无研究价值）
6 轮 → 准确率 0.788   （信号微弱但存在 ← 有研究价值）
12+ 轮 → 准确率 0.5   （痕迹被搅匀，攻不动）
```

**知道"从第几轮开始攻不动"，就等于知道这个密码留了多少安全余量。**

## ② 为什么每个样本用不同的随机密钥？

如果所有样本共用一个密钥，网络可能学会"这个密钥的特征"而不是"差分传播的规律"——那攻击时换个密钥就失效了。

**用不同密钥，逼网络学通用规律**，这样攻击任意密钥都能用。

## ③ 为什么网络只有 10 万参数？

因为它面对的不是"识别猫狗"这种复杂问题，而是**判断一个微弱的统计偏差**。任务单一，不需要大模型。

论文的 SoK 综述指出：**这个领域的模型参数远小于现代深度学习模型**，而且"更大"不一定"更好"（论文提到 8 轮需要特殊技巧，而不是简单加参数）。

---

# 第 5 层：自检清单（能答上这些 = 真懂了）

## 基础层

- [ ] 用一句话说清这个项目在做什么？
- [ ] `--rounds 6` 和 `--epochs 200` 分别是什么意思？区别在哪？
- [ ] `MASK = 0xFFFF` 是干什么的？为什么要用它？

## 中间层

- [ ] 什么是"差分对"？为什么网络能区分它和随机对？
- [ ] 数据生成时为什么要给每个样本不同的密钥？
- [ ] 网络的输入为什么组织成"4 个 16 位字"而不是 64 个独立比特？
- [ ] 为什么用 6 轮区分器能攻击 8 轮？（提示：1 + 6 + 1）
- [ ] loss 和 val_acc 有什么区别？为什么都要看？

## 深入层

- [ ] 残差连接解决了什么问题？（提示：梯度消失）
- [ ] 为什么本项目不用池化层？
- [ ] 循环学习率为什么要周期性变化？
- [ ] 密钥恢复攻击的打分公式为什么用 log₂(Z/(1−Z))？
- [ ] 论文的"命题 1"说了什么？我们的攻击为什么能突破它？

**如果有答不上的，翻回对应的那一层，或者直接问我。**

---

# 第 6 层：学习路径建议

如果你觉得还是有点乱，按这个顺序补：

| 顺序 | 补什么 | 材料 | 耗时 |
|---|---|---|---|
| 1 | **差分分析基础** | `papers/Heys教程中文_2_差分分析.md` | 1 周 |
| 2 | **反向传播原理** | micrograd（154 行纯 Python） | 1 天 |
| 3 | **PyTorch 实操** | mrdbourke 的课程 | 1-2 周 |
| 4 | **攻击细节** | `papers/Gohr2019中文对照_4_*.md` | 3 天 |

**最有效的方法**：**讲给别人听**。挑一个概念（比如"为什么 6 轮网络能攻击 8 轮"），用自己的话讲一遍。讲不通的地方就是你的知识缺口。

---

## 附：文件对照表

| 你想做 | 打开哪个文件 |
|---|---|
| 看加密算法 | `src/speck/speck.py` |
| 看数据怎么造的 | `src/data/dataset.py` |
| 看网络结构 | `src/model/distinguisher.py` |
| 看训练怎么跑的 | `train.py` |
| 看攻击怎么做的 | `key_recovery.py` |
| 看项目原理讲解 | `项目全景与原理详解.md` |
| 看复现全记录 | `docs/复现学习记录.md` |
| 看攻击原理 | `docs/密钥恢复攻击说明.md` |
| **看这份地图** | `学习地图.md`（本文件） |
| 看论文原文与翻译 | `C:\Code\study\papers\` |
