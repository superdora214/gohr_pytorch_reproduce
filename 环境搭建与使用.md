# 环境搭建与日常使用指南

> 结论先说：**你平时根本不需要写那条长长的路径**。那是我的执行环境限制，不是你的。
> 本文档给出 4 种日常用法（从最省事到最通用），以及换机器、上服务器的方案。

---

## 一、你现在的环境状况

| 项目 | 状态 |
|---|---|
| conda | 26.5.3，**PowerShell 已初始化**（可直接 `conda activate`） |
| 环境列表 | `base`、`myenv`、**`pytorch-gpu`（本项目主力）** |
| pytorch-gpu 内容 | Python 3.11.15、torch 2.13.0+cu130、numpy 2.4.4、tensorboard 2.21.0、matplotlib 3.11.1、pillow 12.2.0 ✅ 齐全 |
| VSCode | 已配置解释器 + 终端自动激活 |

**一句话**：环境是健康的，`pytorch-gpu` 一个环境就能跑完本项目所有工作（训练、画图、TensorBoard）。

---

## 二、日常使用：4 种方式（都不用写完整路径）

### 方式 1：VSCode 里点按钮 ⭐ 最推荐

打开项目文件夹后：

- **`Ctrl+shift+P` → `Run Task`** → 选任务（已预置 7 个任务：快速测试 / 5·6·7 轮训练 / 续训 / 画图 / TensorBoard）
- 或者**按 `F5`**（调试面板已预置 6 个配置）
- 或者**打开内置终端**（`` Ctrl+` ``）——它会**自动激活 pytorch-gpu 环境**，然后直接敲：

  ```powershell
  python train.py --rounds 6 --train-size 10000000 --epochs 200 --depth 10
  ```

**这种方式完全不用管环境，VSCode 都帮你激活好了。**

### 方式 2：PowerShell / Windows Terminal（已可用）

```powershell
conda activate pytorch-gpu
cd C:\Code\study\gohr_pytorch_reproduce
python train.py --rounds 6 --train-size 10000000 --epochs 200 --depth 10
```

> 你的 PowerShell 已经初始化过 conda（profile.ps1 里有配置），**直接就能用**。
> 第一行激活一次，之后这个窗口里 `python` 就一直是环境里的那个，不用重复激活。

### 方式 3：双击 bat（已配好，零配置）

| 双击 | 作用 |
|---|---|
| `开始训练.bat` | 交互式菜单（选轮数、样本量） |
| `画训练曲线.bat` | 自动找最新日志出图 |
| `启动TensorBoard.bat` | 网页实时监控 |

这些 bat 内部已经写好了环境路径，**双击即可，不需要任何环境知识**。

### 方式 4：Anaconda Prompt（经典方式）

开始菜单搜 "Anaconda Prompt" → 打开后依次：
```
conda activate pytorch-gpu
cd /d C:\Code\study\gohr_pytorch_reproduce
python train.py ...
```

### 补充：conda run（不激活也能用）

```powershell
conda run -n pytorch-gpu python train.py --rounds 6 --epochs 200
```
适合写自动化脚本时用（不需要先 activate）。

---

## 三、澄清：为什么有些命令里要写完整路径？

因为**执行命令的环境不同**：

| 环境 | 能否 `python` 直接调用 | 原因 |
|---|---|---|
| **你的 PowerShell / VSCode 终端** | ✅ 能 | 已 activation 或已配置 |
| 我给你写命令的 Git Bash | ❌ 不能 | 那个 shell 没初始化 conda，PATH 里只有 base 的 python |

所以我写 `"C:\Users\lmjkj\anaconda3\envs\pytorch-gpu\python.exe" train.py` 是**为了保证命令在任何环境下都能跑通**，而不是说明你也要这么写。

**你自己用的时候，`conda activate pytorch-gpu` 之后直接 `python` 就行。**

---

## 四、换机器 / 上服务器怎么重建环境

### 方案 A：requirements.txt（推荐，跨平台）

项目里已有 `requirements.txt`。在新机器上：

```bash
conda create -n pytorch-gpu python=3.11 -y
conda activate pytorch-gpu

# 先装对应 CUDA 版本的 torch（按新机器的 CUDA 版本选）
pip install torch --index-url https://download.pytorch.org/whl/cu121

# 再装其余依赖
pip install -r requirements.txt
```

**优点**：跨平台（Windows/Linux 都能用）、体积小、易读。

### 方案 B：导出 conda 环境（本机备份用）

```bash
# 导出（含精确版本号）
conda env export -n pytorch-gpu > environment.yml

# 在另一台机器重建
conda env create -f environment.yml
```

**注意**：`conda env export` 默认会写入平台相关的构建号（`build: py311h...`），跨平台（Windows → Linux）会失败。跨平台迁移请用：

```bash
conda env export -n pytorch-gpu --from-history > environment.yml
```

（`--from-history` 只导出你显式安装过的包，不含平台构建号）

### 方案 C：项目本身变成可安装包（长期最佳）

如果以后要把这个仓库给别人用，加一个 `pyproject.toml` 或 `setup.py`，然后：
```bash
pip install -e .
```
这样 `import src.speck` 在任何目录都能用，还可以带一个命令行入口（`gohr-train`）。

---

## 五、Docker：现在不必，什么时候才值得

### 诚实评估：对你当前的情况，Docker 收益低

| 维度 | 情况 |
|---|---|
| 你的机器 | Windows 笔记本 + RTX 5060 8GB |
| Docker 跑 GPU 的前置 | Docker Desktop + WSL2 + NVIDIA Container Toolkit（配置约 2-4 小时） |
| 性能损耗 | WSL2 GPU 直通有 5%-15% 开销（你显存本来就紧张） |
| 解决的问题 | **环境一致性** —— 但你只有一台机器，不存在"环境不一致" |

**结论：现在上 Docker 是"用 4 小时配置换 0 收益"，不值得。**

### 什么时候值得？（三个明确场景）

1. **上组里的服务器** —— 服务器上 Docker + NVIDIA Container Toolkit 是标配，那时学 Docker 是必需品，而且 Linux 下配置比 Windows 简单得多。
2. **论文需要"环境可复现"** —— 有些期刊/会议要求提供可复现的运行环境，Docker 镜像是最强方案。
3. **多项目依赖打架** —— 比如另一个项目要 torch 1.x，与本项目冲突。

### 如果以后要用，给你一个起步模板

```dockerfile
# 用官方 CUDA 基础镜像（省去自己配驱动）
FROM pytorch/pytorch:2.13.0-cuda13.0-cudnn9-runtime

WORKDIR /workspace
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .
# 运行时：docker run --gpus all -v $(pwd):/workspace <image> python train.py ...
```

**关键点**：用 `--gpus all` 才能让容器用上显卡；宿主机的 NVIDIA 驱动必须支持容器内的 CUDA 版本。

### 建议的时机安排

```
现在（研一上）  → 直接用 conda，把 requirements.txt 维护好
寒假           → 有空可以玩一下 Docker（装个 CPU 版练手）
上服务器前      → 正式学 Docker + GPU 容器（那时 Linux 环境配置简单）
```

---

## 六、常见问题

**Q1：`conda activate` 报错 "CommandNotFoundError"？**
→ 说明该终端没初始化 conda。解决：`conda init powershell`（或 `conda init bash`），然后**重开终端**。

**Q2：激活了环境，但 `python` 还是指向 base？**
→ 用 `where python` 检查实际路径。若不对，说明 PATH 顺序有问题，或者激活失败（看提示符前面有没有 `(pytorch-gpu)`）。

**Q3：怎么确认当前用的是哪个环境？**
```powershell
python -c "import sys; print(sys.executable)"
```
或看终端提示符前面有没有 `(pytorch-gpu)`。

**Q4：base 环境能不能直接用来跑这个项目？**
→ 最好不要。base 里可能没有 CUDA 版的 torch。**养成"项目用专属环境"的习惯**，这也是为什么要有 `conda activate`。

**Q5：多个项目怎么办？**
→ 一个项目一个环境：`conda create -n 项目名 python=3.11`。这样互不干扰，也方便以后分别导出依赖。

**Q6：环境装坏了想重来？**
```powershell
conda env remove -n pytorch-gpu
# 然后按"第四节"重建
```
（前提是你维护好了 requirements.txt —— 这也说明了它的重要性）
