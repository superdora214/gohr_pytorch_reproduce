# 怎么自己开始训练（操作手册）

> 目录：`C:\Code\study\gohr_pytorch_reproduce\`
> 适用：Gohr 神经区分器的训练（train.py）

---

## 一、先搞清楚：训练的是什么、在 Gohr 复现里的位置

Gohr 复现分三步：

```
第1步 ✅ SPECK32 加密实现        src/speck/speck.py（官方向量验证通过）
第2步 🔄 训练神经区分器          ← 本文档讲的就是这一步（train.py）
第3步 ⬜ 密钥恢复攻击            还没做
```

**第2步在造什么**：一个二分类神经网络，输入一对密文（64个bit），输出 0~1 的数——
越接近 1 表示"越确信这对密文来自一对只差一个比特的明文（真实差分对）"。

**训练数据从哪来**：GPU 上现场生成 1000 万对密文（一半真实差分对、一半随机对），
每对都用**独立的随机密钥**加密，所以网络学不到"背密钥"，只能学差分结构本身。

**训练成功的标志**：
| 轮数 | 论文基准 | 说明 |
|---|---|---|
| 1 轮 | ~1.00 | 几乎裸奔 |
| 3 轮 | ~0.99 | 仍然容易 |
| 5 轮 | ~1.00（论文） | 信号开始变弱 |
| 6 轮 | **~0.72** | 当前目标 |
| 7 轮 | ~0.58 | 更接近噪声 |

只要准确率明显高于 0.50（随机猜），就证明该轮数下存在可被神经网络检测的统计破绽。

---

## 二、最简单的方式：双击启动器

👉 **双击 `开始训练.bat`**，会弹出菜单：

```
  [1] 快速测试（3轮/10万样本/3epoch/约2分钟）
  [2] 训练 5 轮（论文级/1000万样本/200epoch）
  [3] 训练 6 轮（论文级/1000万样本/200epoch）
  [4] 训练 7 轮（论文级/1000万样本/200epoch）
  [5] 续训上一次（--resume）
```

- 新手建议先用 **[1] 快速测试**跑通一遍（2 分钟就出结果），确认流程无误再跑论文级。
- 论文级一次约 18 小时（每 epoch 约 5.6 分钟 × 200）。**训练窗口不能关**，关掉就等于停止。

---

## 三、手动方式（命令行，5 步）

```powershell
# ① 打开 PowerShell，进入项目目录
cd C:\Code\study\gohr_pytorch_reproduce

# ② 用 pytorch-gpu 环境的 python（关键：不是默认的 python）
$PY = "C:\Users\lmjkj\anaconda3\envs\pytorch-gpu\python.exe"

# ③ 先跑个快速测试，确认环境没问题
& $PY train.py --rounds 3 --train-size 100000 --epochs 3 --depth 3

# ④ 确认无误后，跑正式训练（6轮论文级）
& $PY train.py --rounds 6 --train-size 10000000 --eval-size 1000000 --epochs 200 --depth 10

# ⑤ 想看实时曲线：另开一个窗口
& $PY -m tensorboard.main --logdir checkpoints --port 6006
#   然后浏览器打开 http://localhost:6006
```

---

## 四、参数速查表

| 参数 | 含义 | 建议值 |
|---|---|---|
| `--rounds` | SPECK 加密轮数 | 3（测试）/ 5 / 6 / 7 |
| `--train-size` | 训练样本数 | 测试 10万；论文级 1000万 |
| `--eval-size` | 验证样本数 | 测试 2万；论文级 100万 |
| `--epochs` | 训练轮数 | 测试 3；论文级 200 |
| `--depth` | 网络残差块深度 | 测试 3；论文级 10（论文设定） |
| `--batch-size` | 每批样本数 | 默认 5000（显存满就别加大） |
| `--resume` | 从断点续训 | 中断后用 |
| `--no-tensorboard` | 关闭 TensorBoard 记录 | 一般不用 |
| `--outdir` | 输出目录 | 默认 ./checkpoints |

**显存关系**：`train-size` 决定显存占用（1000万样本约 2.6GB），加上网络和 batch，
8GB 显存下 1000万 + batch 5000 已经是上限（实测占用 7.7/8.1GB）。

---

## 五、常见场景（复制即用）

```powershell
$PY = "C:\Users\lmjkj\anaconda3\envs\pytorch-gpu\python.exe"
cd C:\Code\study\gohr_pytorch_reproduce

# 冒烟测试：2 分钟，只验证流程
& $PY train.py --rounds 3 --train-size 100000 --eval-size 20000 --epochs 3 --depth 3

# 复现论文 5 轮（0.997 基准）
& $PY train.py --rounds 5 --train-size 10000000 --eval-size 1000000 --epochs 200 --depth 10

# 复现论文 6 轮（0.72 基准）
& $PY train.py --rounds 6 --train-size 10000000 --eval-size 1000000 --epochs 200 --depth 10

# 复现论文 7 轮（0.58 基准）
& $PY train.py --rounds 7 --train-size 10000000 --eval-size 1000000 --epochs 200 --depth 10

# 中断后继续（例如 6 轮跑到了 epoch 50 被中断）
& $PY train.py --rounds 6 --train-size 10000000 --eval-size 1000000 --epochs 200 --depth 10 --resume
```

> ⚠️ 同一时间只跑一个训练（显存不够第二个）。想换配置，先停掉当前的。

---

## 六、中断了怎么继续

```powershell
& $PY train.py --rounds 6 --train-size 10000000 --epochs 200 --depth 10 --resume
```

`--resume` 会自动读回：
- 网络权重
- **优化器状态**（Adam 的动量，没有它续训效果会打折）
- 上次跑到第几个 epoch

所以它会从断点接着跑，不重跑。

**保险机制**：每个 epoch 都会存两份：
- `checkpoints/last_6r.pt` —— 最后状态（续训用，保证不丢进度）
- `checkpoints/net6r.pt` —— 历史最佳（做实验/攻击时加载这份）

---

## 七、训练时看什么

| 想看什么 | 怎么做 |
|---|---|
| 实时文字进度 | 训练窗口本身就在滚；或 `tail` 日志文件 |
| 曲线图 | 双击 `画训练曲线.bat` → 生成 `training_curves.png` |
| 网页实时监控 | 双击 `启动TensorBoard.bat` → 浏览器开 `http://localhost:6006` |
| 原始数据 | `checkpoints/history_6r.csv`（Excel 可直接打开） |

**训练中要看的三件事**：
1. `val_acc` 是否在涨（前几个 epoch 可能平，等学习率循环几个周期后会跳）
2. `loss` 是否在降
3. 如果 20 个 epoch 后 acc 完全不动 → 配置有问题（样本太少/轮数太高）

---

## 八、排错

| 现象 | 原因与处理 |
|---|---|
| `CUDA out of memory` | 显存不够：减小 `--train-size` 或 `--batch-size` |
| `SPECK 测试向量未通过` | 加密实现被改坏了，检查 `src/speck/speck.py` |
| TensorBoard 打不开 | 端口 6006 被占：换 `--port 6007` |
| 曲线图打不开 | 已是 RGB 格式；若仍不行用 VSCode/浏览器打开 PNG |
| 训练被睡眠中断 | 运行 `设置不休眠-右键管理员运行.bat`；别合笔记本盖子 |
| 想换 6 轮但显存不够 | 降到 500 万样本（`--train-size 5000000`），精度会略降 |

---

## 九、一句话流程总结

```
双击 开始训练.bat  →  选 [1] 先测试  →  确认无误  →  选 [3] 跑 6 轮  →  双击 启动TensorBoard.bat 看曲线
   中断了？ →  选 [5] 续训
```
